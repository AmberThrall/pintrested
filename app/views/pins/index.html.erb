<p id="notice"><%= notice %></p>

<h1>Pins</h1>

<div id="pins" class="transitions-enabled infinite-scroll clearfix">
  <% @pins.each do |pin| %>
    <div class="pin">
      <div class="panel panel-default">
        <%= link_to image_tag(pin.image.url(:medium)), pin %><br />
        <div class="panel-body">
          <%= pin.description %>
        </div>

        <div class="panel-footer">
          <% if pin.user == current_user %>
            <%= link_to 'Edit', edit_pin_path(pin) %><br />
            <%= link_to 'Destroy', pin, method: :delete, data: { confirm: 'Are you sure?' } %><br />
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>

<div class="center">
  <%= will_paginate @pins, renderer: BootstrapPagination::Rails %>
</div>

<script src="https://unpkg.com/imagesloaded@4/imagesloaded.pkgd.min.js"></script>
<%= javascript_include_tag('pins', 'data-turbolinks-track': 'reload') %>
